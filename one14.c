//Author: Abs_;
/* Write a program to display the equation of a line in the form ax+by=c for a = 5, b = 8 and c = 18. */
// very simple problem, the problem don't require any input statement but we will use it;
#include <stdio.h>
void main()
{
    int a = 5, b = 8, c = 18; // default values given in statement.
    //scanf("%d %d %d", &a, &b ,&c); // if you want to take input then use it.
    printf("\n %dx + %dy = %d", a, b, c);
}